/** Global site settings */
export const siteConfig = {
  /** Site name / brand */
  name: 'NEXUS',
  /** Tagline / subtitle */
  tagline: 'Multiverse Archive',
  /** Default page title suffix */
  titleSuffix: 'NEXUS',
  /** Base URL for the site (change in production) */
  baseUrl: '/',
  /** Current user display */
  user: {
    name: 'Archivist',
    level: 12,
    avatarVisible: true,
  },
} as const;
