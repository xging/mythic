# Project Instructions

Before making changes, follow AGENT_RULES.md.

Main rules:

- libs/ui contains only universal UI components.
- Do not add domain-specific DND or character logic into libs/ui.
- Card is only a visual container and must use children.
- Use CSS variables for styling.
- Domain-specific components belong in apps/\*.
